/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ["source.unsplash.com", "drive.google.com", "media.licdn.com", "github.com", "ui-avatars.com"],
  },
};

export default nextConfig;
